# Job Search API - Backend Documentation

Веб-сервис для поиска работы и подбора персонала в Таджикистане.

> 📖 **Для фронтенд разработчиков**: См. [README_FRONTEND.md](README_FRONTEND.md)

## Технологический стек

- **Python** 3.10+
- **Django** 5.2.8
- **Django REST Framework**
- **SQLite** (база данных)
- **JWT** (djangorestframework-simplejwt) с refresh-токенами и blacklist
- **Swagger UI** (drf-yasg) - документация API по адресу `/docs/`

## Установка и настройка

### 1. Клонирование и создание виртуального окружения

```bash
# Создание виртуального окружения
python -m venv .venv

# Активация виртуального окружения
# Windows:
.venv\Scripts\activate
# Linux/Mac:
source .venv/bin/activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или установите вручную:

```bash
pip install django==5.2.8
pip install djangorestframework
pip install djangorestframework-simplejwt
pip install drf-yasg
pip install Pillow
pip install python-dotenv
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта (можно скопировать из `.env.example`):

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

Отредактируйте `.env` файл и укажите свои значения:

```env
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
ACCESS_TOKEN_LIFETIME_MINUTES=15
REFRESH_TOKEN_LIFETIME_DAYS=1
```

⚠️ **Важно**: Файл `.env` уже добавлен в `.gitignore` и не будет попадать в репозиторий.

### 4. Применение миграций

```bash
python manage.py migrate
```

### 5. Создание суперпользователя

```bash
python manage.py createsuperuser
```

### 6. Запуск сервера разработки

```bash
python manage.py runserver
```

Сервер будет доступен по адресу: `http://127.0.0.1:8000/`

## Структура проекта

```
19.11(class)/
├── accounts/          # Приложение для аутентификации
│   ├── models.py      # CustomUser модель
│   ├── views.py       # API views для регистрации/входа
│   ├── serializers.py # Сериализаторы для пользователей
│   └── urls.py        # URL маршруты для auth
├── api/               # Основное приложение API
│   ├── models.py      # Company, Vacancy, Resume, Application, FavoriteVacancy, Skill
│   ├── views.py       # API views для вакансий, резюме, откликов
│   ├── serializers.py # Сериализаторы для всех моделей
│   └── urls.py        # URL маршруты для API
├── server/            # Настройки проекта
│   ├── settings.py    # Конфигурация Django
│   └── urls.py        # Главный URL конфиг
└── manage.py          # Django management script
```

## Модели данных

### CustomUser (accounts/models.py)
- Наследуется от `AbstractUser`
- Поле `role`: `seeker` (соискатель) или `employer` (работодатель)

### Company (api/models.py)
- `name` - название компании
- `logo` - логотип (ImageField)
- `description` - описание
- `website` - сайт компании

### Vacancy (api/models.py)
- `title` - название вакансии
- `company` - ForeignKey к Company
- `location` - местоположение
- `description` - описание
- `responsibilities` - обязанности
- `requirements` - требования
- `salary_from`, `salary_to` - зарплата
- `currency` - валюта (по умолчанию TJS)
- `employment_type` - тип занятости
- `work_format` - формат работы
- `experience_required` - требуемый опыт
- `is_active` - активна ли вакансия
- `views` - количество просмотров
- `author` - ForeignKey к CustomUser (работодатель)

### Resume (api/models.py)
- `user` - OneToOne с CustomUser (соискатель)
- `full_name` - полное имя
- `desired_position` - желаемая должность
- `location` - местоположение
- `phone`, `email` - контакты
- `salary_expectation` - ожидаемая зарплата
- `experience_years` - годы опыта
- `about` - о себе
- `skills` - ManyToMany к Skill
- `is_active` - активно ли резюме

### Application (api/models.py)
- `applicant` - ForeignKey к CustomUser (соискатель)
- `vacancy` - ForeignKey к Vacancy
- `resume` - ForeignKey к Resume (опционально)
- `cover_letter` - сопроводительное письмо
- `status` - статус (pending, reviewed, accepted, rejected)
- `unique_together`: (applicant, vacancy) - защита от повторных откликов

### FavoriteVacancy (api/models.py)
- `user` - ForeignKey к CustomUser
- `vacancy` - ForeignKey к Vacancy
- `unique_together`: (user, vacancy)

### Skill (api/models.py)
- `name` - название навыка (уникальное)

## API Эндпоинты

### Аутентификация (`/auth/`)

| Метод | URL | Описание | Доступ |
|-------|-----|----------|--------|
| POST | `/auth/register/` | Регистрация нового пользователя | Все |
| POST | `/auth/login/` | Вход → возвращает access + refresh токены | Все |
| POST | `/auth/refresh/` | Обновление access-токена | Все |
| POST | `/auth/logout/` | Выход (blacklist refresh-токена) | Авторизованные |

**Пример регистрации:**
```json
POST /auth/register/
{
    "username": "employer1",
    "email": "employer@example.com",
    "password": "password123",
    "confirm_password": "password123",
    "role": "employer"
}
```

**Пример входа:**
```json
POST /auth/login/
{
    "username": "employer1",
    "password": "password123"
}

Response:
{
    "refresh": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "access": "eyJ0eXAiOiJKV1QiLCJhbGc..."
}
```

### Вакансии (`/api/vacancies/`)

| Метод | URL | Описание | Доступ |
|-------|-----|----------|--------|
| GET | `/api/vacancies/` | Список всех вакансий | Все |
| POST | `/api/vacancies/` | Создание вакансии | Только employer |
| GET | `/api/vacancies/?t=python` | Поиск по названию | Все |
| GET | `/api/vacancies/?d=2025-11-21` | Фильтр по дате | Все |
| GET | `/api/vacancies/<pk>/` | Детали вакансии (views++) | Все |
| PUT | `/api/vacancies/<pk>/` | Редактирование вакансии | Только автор |
| DELETE | `/api/vacancies/<pk>/` | Удаление вакансии | Только автор |

**Пример создания вакансии:**
```json
POST /api/vacancies/
Headers: Authorization: Bearer <access_token>
{
    "title": "Python Developer",
    "company_id": 1,
    "location": "Душанбе",
    "description": "Описание вакансии",
    "salary_from": 5000,
    "salary_to": 8000,
    "currency": "TJS",
    "employment_type": "full_time",
    "work_format": "remote",
    "experience_required": "1_3"
}
```

### Резюме (`/api/resumes/`)

| Метод | URL | Описание | Доступ |
|-------|-----|----------|--------|
| GET | `/api/resumes/` | Список активных резюме | Все |
| POST | `/api/resumes/` | Создание резюме | Только seeker |
| GET | `/api/resumes/?skill=Django` | Поиск по навыку | Все |
| GET | `/api/resumes/<pk>/` | Детали резюме | Все |
| PUT | `/api/resumes/<pk>/` | Редактирование резюме | Только владелец |
| DELETE | `/api/resumes/<pk>/` | Удаление резюме | Только владелец |

**Пример создания резюме:**
```json
POST /api/resumes/
Headers: Authorization: Bearer <access_token>
{
    "full_name": "Иван Иванов",
    "desired_position": "Python Developer",
    "location": "Душанбе",
    "phone": "+992901234567",
    "email": "ivan@example.com",
    "salary_expectation": 6000,
    "experience_years": 2,
    "about": "О себе...",
    "skill_ids": [1, 2, 3]
}
```

### Отклики (`/api/vacancies/<vacancy_id>/apply/`)

| Метод | URL | Описание | Доступ |
|-------|-----|----------|--------|
| POST | `/api/vacancies/<vacancy_id>/apply/` | Откликнуться на вакансию | Только seeker |

**Пример отклика:**
```json
POST /api/vacancies/1/apply/
Headers: Authorization: Bearer <access_token>
{
    "cover_letter": "Я заинтересован в этой вакансии...",
    "resume_id": 1
}
```

### Избранное (`/api/vacancies/<vacancy_id>/favorite/`)

| Метод | URL | Описание | Доступ |
|-------|-----|----------|--------|
| POST | `/api/vacancies/<vacancy_id>/favorite/` | Добавить/убрать из избранного (toggle) | Только seeker |

**Пример:**
```json
POST /api/vacancies/1/favorite/
Headers: Authorization: Bearer <access_token>

Response (добавлено):
{
    "message": "Added to favorites"
}

Response (удалено):
{
    "message": "Removed from favorites"
}
```

## Безопасность

### JWT Токены
- **Access токен**: 15 минут жизни
- **Refresh токен**: 1 день жизни
- **Blacklist**: refresh-токены при выходе заносятся в blacklist

### Разрешения
- `IsAuthenticatedOrReadOnly` - чтение доступно всем, создание/изменение только авторизованным
- Проверка ролей:
  - Только `employer` может создавать вакансии
  - Только `seeker` может создавать резюме, откликаться и добавлять в избранное
- Проверка владельца:
  - Редактирование/удаление вакансий - только автор
  - Редактирование/удаление резюме - только владелец

## Особенности реализации

### Автоматическое увеличение просмотров
При получении деталей вакансии (`GET /api/vacancies/<pk>/`) счетчик `views` автоматически увеличивается на 1.

### Метод salary_display()
Возвращает красиво отформатированную строку зарплаты:
- "5000 – 8000 TJS" (если указаны оба значения)
- "от 5000 TJS" (если только from)
- "до 8000 TJS" (если только to)
- "По договорённости" (если show_salary=False)
- "Не указана" (если ничего не указано)

### Защита от повторных откликов
Модель `Application` имеет `unique_together = ("applicant", "vacancy")`, что предотвращает повторные отклики одного пользователя на одну вакансию.

### Toggle избранного
Один эндпоинт `/api/vacancies/<vacancy_id>/favorite/` добавляет вакансию в избранное, если её там нет, или удаляет, если она уже есть.

## Админ-панель

Доступна по адресу: `http://127.0.0.1:8000/admin/`

Все модели зарегистрированы в админке:
- CustomUser
- Company
- Vacancy
- Resume
- Application
- FavoriteVacancy
- Skill

## Swagger документация

Интерактивная документация API доступна по адресу:
`http://127.0.0.1:8000/docs/`

В Swagger UI можно:
- Просмотреть все эндпоинты
- Увидеть схемы запросов/ответов
- Протестировать API прямо в браузере
- Авторизоваться через JWT токены

## Миграции

Создание новых миграций:
```bash
python manage.py makemigrations
```

Применение миграций:
```bash
python manage.py migrate
```

## Тестирование

Запуск тестов:
```bash
python manage.py test
```

## Развертывание в production

⚠️ **Важно**: Текущий сервер разработки (`runserver`) НЕ подходит для production!

Для production используйте:
- **Gunicorn** или **uWSGI** (WSGI сервер)
- **Nginx** (обратный прокси)
- **PostgreSQL** вместо SQLite
- Настройте `DEBUG = False`
- Настройте `ALLOWED_HOSTS`
- Используйте переменные окружения для `SECRET_KEY`

## Полезные команды

```bash
# Создание суперпользователя
python manage.py createsuperuser

# Создание миграций
python manage.py makemigrations

# Применение миграций
python manage.py migrate

# Запуск сервера
python manage.py runserver

# Открыть Django shell
python manage.py shell

# Собрать статические файлы (для production)
python manage.py collectstatic
```

## Контакты и поддержка

Для вопросов и предложений обращайтесь к разработчику проекта.

